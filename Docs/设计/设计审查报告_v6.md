# V4 设计稿审查报告（核查修正版 · 46 屏）

> **状态（2026-07-06 更新）**：中高危项已随审查修复线波次④落地（红→蓝/紫→主蓝、图标盒圆角方+按钮胶囊、48dp 触控等，见 [`../CHANGELOG.md`](../CHANGELOG.md) v10）；遗留 ⏳ 项（暖色"动作 vs 状态"精细分离、主蓝统一 token、Metric/页头模板统一）仍为活缺口。
>
> **核查修正（2026-06-25）**：原报告由 13 个视觉 agent + 1 个一致性 agent 产出。本次逐屏比对 `设计稿画廊_v6_files/design_NN.png` 截图与原型源码 `prototypes/v4_android.html`（并用 codex 独立交叉验证），修正了**第三节的大部分误判**、**第二节 design_09 的定性错误**、**第四节的自相矛盾**。
>
> **方法学要点**：截图只截原型的 `.phone` 框；紧跟其后的 `.screen-note` / `.screen-flow` / `.screen-ux` 是**屏外注释块，不进截图**。判断"屏内是否有说明性副标"必须以截图为准——原第三节多处把屏外注释当成了屏内副标。

源图 `设计稿画廊_v6_files/design_NN.png`，画廊 `设计稿画廊_v6.html`。

## 一、截图保真：干净，无真实采集 bug ✅（原结论成立）
逐屏检查 46 张，**没有发现真正的截图缺陷**（无错位裁切、无半屏/双屏、无行高塌陷）。workflow 标的 2 处「重大」，核实后都是**原型本身的渲染**、截图忠实还原，不是截图管线的问题：

- **design_39 设备维护（DUT）** — 整屏发灰 + 中央「后期实现」气泡。这是原型给「后期/二期」屏的**有意占位态**（dim + badge），忠实截下来的。
- **design_20 数据采集·就绪** — log 区「[时间戳] HEX」占位示例与居中空态文案视觉相邻/轻叠。这是**原型 log 空态的布局**（一个格式提示 + 一个空提示挤在窄框里），非截图 bug；若要消除需改原型那屏。

⚠️ **纳入口径不一致**：design_39 是占位「后期实现」屏，却被纳入 46 屏；而另外 5 张占位屏（GNSS A/B/C、配置A、配置B）已被排除。建议统一——要么 design_39 也排除，要么 5 屏一并纳入。

## 二、设计一致性（横向比对，按严重度）✅ 基本成立，1 处已修正

| 严重度 | 领域 | 问题 | 建议 |
|---|---|---|---|
| **major** | 状态色语义 | **① 红色被多义复用**：心率「♥」用红（design_20「♥72」、design_21「♥71」），而 design_32 红=错误。**② 暖色被多义复用**：橙/黄既做警示动作 CTA（design_09「去开启」橙按钮），又做「连接中」状态 pill 与「参考」标（design_14）。〔修正：design_09 的橙是**警示动作按钮**，非"进行中/连接中"状态——原报告归类有误〕 | 固定四态色板：就绪/成功=绿、进行中=单选一种、警告=橙、错误=红；指标数值（含心率♥）不用纯红，红独占错误；暖色区分"动作"与"状态"两套或收敛 |
| **major** | 主按钮 | 同为页面主 CTA，有的实心有的描边：design_36「清理已导出会话」用描边当主操作；主蓝色值多屏冷暖深浅不一 | 主蓝收敛到单一 token；主操作=实心胶囊、次=描边、危险=红，固定 |
| **major** | 选择控件 | 「选中」三种画法：design_17 紫勾、design_40 蓝圆点(radio)、design_28 蓝勾；且**单选**就有两种画法（17 勾 vs 40 radio），选中色紫/蓝混用 | 单选用 radio、多选用 checkbox/勾；选中强调色全局统一为主蓝 |
| minor | 页头模板 | 「大标题+副标」(design_01)与「返回箭头+标题」(多数详情页)两套，副标字号/颜色/是否带说明文不一 | 定 1–2 套页头模板，副标统一 token |
| minor | 实体→颜色 | 「采集场景」图标底色 design_01 绿、design_07 蓝，同概念不一致（文件五段色已统一，保持） | 做「实体→颜色」固定映射表全局套用 |
| minor | metric 展示 | 大数字(计时/行数/占用/wallclock)各屏字号·单位比例不一，无统一组件 | 抽 metric 组件固定数值/单位字号比 |
| minor | 文案·中英 | 连接态动词不统一(已连接/连接中/重连)；中英混用无明文口径 | 定术语表；明确「标签层中文、值层英文」 |
| minor | 文案·标点 | 并列分隔符中点·与斜杠/混用，斜杠两侧空格不一 | 枚举用·，二选一关系用/并固定空格 |
| minor | status pill | 「就绪」(绿)「已连」(蓝)「OK」(绿) 三套 pill：圆角/内边距/是否带前导圆点不一 | 统一 pill 组件，四态复用同一色板 |
| minor | 图标语义 | 「采集场景」概念在 design_01(波形)/design_07(靶心) 用了不同图标 | 同概念固定一个图标，维护图标-语义表 |
| info | 留白 | 短内容页(design_01/36/40/41)顶部堆叠+底部大留白，偏空 | 可垂直居中或加引导占位（低优先级） |

> 备注：红心率用 ♥ 红是通用惯例，定 major 偏重，可降级或仅约束"红不得同时表示错误"。

## 三、屏内说明性副标 —— 按截图重核（原结论大半撤销）

口径：屏内不放说明性副标（规格/口径/数据流的解释放注释区或代码注释）。**以截图为准**逐屏核查后，原第三节 11 屏里只有 2 屏成立、1 屏是另一类问题，其余 8 屏属误判（文字其实在屏外注释，或屏内是功能性状态条/必要弹层文案）。

### ✅ 成立——屏内确有可精简内容
- **design_13 后台省电** — 屏内有 info 条「采集需长时间后台运行…请切到不省电/不受限」+ 副标「保活·防采集被系统杀」+ 磁贴说明（国产 ROM:自启动管理 / 上锁防一键清理后台）。**精简**：保留必要操作项，说明下沉。
- **design_46 全局提示条** — 屏内「锁定/进行中」三处重复：顶部全局条（正在采集…）、副标「会话进行中·入口锁定」、磁贴「采集中不可…/锁定」。**去重**：留全局条，删冗余副标。
- **design_38 应用日志**（修正定性）— 屏内有一张只复读标题「应用日志」的 **info 卡（无实义、纯冗余）**，并非"说明卡"。**直接删**这张冗余卡。

（可选低优先）design_01 等首页的功能性 tagline「多传感器 BLE 数据采集·准备会话」「采集用户信息」属可清理的说明性副标。

### ❌ 撤销——原报告误判（屏内并无该文字 / 文字在屏外注释）
| 屏 | 原报告主张 | 截图核查结论 |
|---|---|---|
| design_15 上限 | 「最多3台+参考心率带≤1」规格副标 | 屏内是**功能性黄条**「DUT 已连3台(上限)·链路可能不稳定」（状态反馈，合理）；该规格串在屏外 `.screen-note`（v4_android.html:1338） |
| design_18 用户空态 | 「别名写入文件名与 manifest…」 | 屏内副标是「本地存储·不上传」；cited 串在屏外 `.screen-note`（:1420） |
| design_19 用户编辑 | 「别名写入文件名与 manifest…」 | 屏内是**干净表单**（别名/性别/出生/身高体重）；cited 串在屏外 `.screen-note`(:1494)/`<li>`(:1500) |
| design_37 关于 | 「档案仅本地存储…不出设备」 | 屏内只有版本卡 + 「二期能力(当前不可用)」列表；cited 串在屏外 `.screen-ux <li>`（:2160） |
| design_42 / 43 / 44 横切 | 「整段规格正文」 | 屏内只有**状态条**（重连中 / 存储满）+ 指标卡 / 日志行；**无整段规格正文** |
| design_45 进程恢复 | 「按 Service UUID 定向重扫」机制说明 | 屏内是**恢复确认弹层文案**（会话被中断…设备地址命中可续采，合理）；「定向重扫」在屏外注释（:2434），且全文无"Service UUID" |

## 四、逐屏严重度概览（已修正自相矛盾）
- **none（无问题）**：08,10,17,**18**,**19**,21,22,23,24,28,30,31,40,41
  - 〔修正：18/19 第三节指控已撤销，确归 none；design_21 的红心率属第二节**系统级**色彩问题，逐屏单看可视作 none〕
- **minor（仅小建议）**：01,02,03,04,05,06,07,09,11,12,13,14,15,16,25,26,27,29,32,33,34,35,36,37,**38**,42,43,44,45,46
  - 〔design_38 实为**冗余空 info 卡**，建议删，仍计 minor〕
- **截图忠实但属占位/未实现态**：20（原型 log 空态布局）、39（后期实现占位屏，另见第一节纳入口径问题）

## 五、整改优先级（修正后）
1. **第二节 3 项 major**：色板四态收敛（含暖色"动作/状态"分离、红独占错误）、主按钮实心/描边/危险三态固定、选择控件统一（单选 radio·多选勾·选中色统一主蓝）。
2. **第三节真问题**：design_38 删冗余卡、design_46 去重锁定副标、design_13 精简省电说明。
3. **纳入口径**：统一 design_39 与其余占位屏的取舍。
4. minor/info 项按需排期。

## 六、本轮修复执行（2026-06-25）
已按第五节落地到 `prototypes/v4_android.html` 并重截对应设计稿（尺寸不变 990×2148）。

**§2 三 major —— 已做高置信部分**
- ✅ **选择控件统一**：design_17 选中态 紫勾+紫边框 → **主蓝**（蓝勾 `chk on`＋`--primary-c` 边框）；用户图标仍保留紫作实体色。
- ✅ **主按钮**：design_36「清理已导出会话」`btn block outline`（描边）→ **实心**主按钮。
- ✅ **色板·红独占错误**：心率 ♥（design_20/21 等 3 处 mono span）由 `--error` 红 → `--primary-deep` 蓝，红让位给错误态。
- ⏳ **未做（偏 sweeping，需进一步设计裁断）**：暖色「动作 vs 状态」分离（design_09 橙按钮 / design_14 橙·黄 pill 两套语义）、主蓝多值收敛到单一 token。

**§3 三处真问题 —— 已做**
- ✅ **design_38**：删掉只复读标题的冗余 info 卡。
- ✅ **design_46**：删冗余副标「会话进行中·入口锁定」（保留顶部全局提示条）。
- ✅ **design_13**：删副标「保活·防采集被系统杀」+ info 条 +「忽略电池优化/自启动/锁后台」三磁贴说明、sec-h 去括注，只留 3 个操作项；解释已在屏外 `.screen-flow`。

**未动（按既定 / 低优先级）**
- design_39 占位屏取舍：维持先前「保留（46 屏）」的决定。
- §2 minor/info（页头模板、实体→色映射、metric 组件、文案中英/标点、pill 统一、图标语义、留白）：按需排期。
