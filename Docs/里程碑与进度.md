# BlueTrace 里程碑与进度

> 状态标记：✅ 完成（已真机 / 单测验证）· 🔄 进行中 · ⬜ 待办 · ⏸ 暂不实现（一期范围外）
> 真源：[`/SPEC.md`](../SPEC.md)（§10.5 P1–P5）+ [`prototypes/v4_android.html`](prototypes/v4_android.html)。逐版修改记录见 [`CHANGELOG.md`](CHANGELOG.md)。最后更新：**2026-07-06**。
>
> **一句话现状**：一期 Android app（KMP，`io.bluetrace`）**核心闭环全部打通并真机验证（M1–M6）**；2026-07 又完成三条增量线——**真实 BLE + S7 手表控制台**（默认真实 GATT，DEBUG 可切 Mock）、**四波代码审查修复 v8–v10**（数据安全/接口债/反馈契约/UI 对齐，真机回归 10 项全过）、**协议规格文档线**（自研帧规格开发者版 + S7 协议共识稿 + UWTP V0.99 冻结候选）、**架构演进线 v11**（波次A/B：P0 线程修正 + CI + 注册式协议架构 02 R1–R3 落码，HRS 已可解）。**唯一硬缺口仍是 M7（DUT 采集协议解码）**——2026-07-06 口径修正：**在现网 B2A 主体上扩展采集能力**（UWTP 仅思考方向），扩展方案待重新构思；iOS 与服务器属二期。

---

## 阶段里程碑

### ✅ M1 · KMP 骨架 + 闭环（P1–P2）
- KMP 两模块 `:shared`(commonMain，纯 Kotlin) + `:app`(Android)；Compose + Material3 adaptive、类型安全 Navigation（三 Tab 各独立嵌套 NavGraph + 返回栈）、Koin、DataStore。
- **Mock BLE**（`BleClient` 接口 + `MockBleClient`）：造假 DUT + 标准心率带 + 持续造数据。
- **扁平设备 + 限额**（DUT≤3 + 参考≤1）、用户(Subject) CRUD、三态采集运行状态机、采集类型选择、硬锁定 + 长按 2 秒结束、实时流 `[unixMs] HEX`、Pin/Start-Stop 标签。

### ✅ M2 · 数据落地 + 导出（P3）
- **D-6 会话文件夹**：raw HEX(source of truth) + 每模块解码 CSV + 组合包 CSV + `session_manifest.json` + `gps.csv`。
- **MediaStore 导出** → `Download/BlueTrace/`；数据 Tab 列表/详情/多选/搜索。
- **本机 GNSS（F-GPS-1）**：真实 `LocationManager`（while-in-use）→ `gps.csv`，真机实测真实经纬度。

### ✅ M3 · 后台 / 进程恢复（P4）
- 前台服务托管采集 + 常驻通知；进程恢复（续采 / 开口会话自动收尾）；存储预检全链；权限永久拒绝引导；采集中关蓝牙→暂停·自动重连。

### ✅ M4 · 产品化打磨
- 品牌图标 + 两层启动屏（系统层底色 + 应用内 `AppSplash`，冷启一次）；全局外观模式（亮/暗/跟随系统，DataStore 持久、即时生效）；GNSS 入口迁移（设置开关 → 采集类型勾选，每会话就地授权）；i18n（zh + values-en + plurals）、edge-to-edge、演示钩子 DEBUG 门控。

### ✅ M5 · 设计↔实现逐屏同步（v3 → v6）
> 以 `prototypes/v4_android.html` 为设计真源，分轮把实现对齐原型并**逐屏真机验证**。详见 [`CHANGELOG.md`](CHANGELOG.md)。
- **v3**：采集A / 用户A / 设备A / 设置 收敛（EntryTile 重构等）；裁决 GNSS 入口、性别英文枚举。
- **v4**：GNSS 线收尾 + 入口迁移到采集类型勾选。
- **v5**：主界面三屏对齐——采集场景 tile + 在线/离线双采集（删说明性副标）、数据 Tab 删模式筛选、设置「外观」→「通用」+ 新增语言屏；红线#1（屏内零说明性副标）。
- **v6**：把"采集场景"从占位变 **JSON 驱动真模型**——
  - 场景模型（`Scene/SceneSelection/SceneCatalog`，`scenes.json` 驱动，默认取第一项、选择持久化）；
  - **5 段文件命名**（`主场景_子场景_用户_日期_时间_MAC后四位`，token 恒英文）+ manifest 记 `mainScene/subScene`；
  - 场景选择页（主·子两级单选）、用户选择重设计（Default 行 + 单选 + 确认 + 行内编辑/删除）、用户编辑重设计（别名仅英数 + 出生 DatePicker + 身高/体重 Slider）；
  - 结束摘要 / 会话详情新增「✎ 修改采集人/场景」→ 改 manifest + 重命名文件夹；
  - 真机硬门：M2101K9C 全部关键路径跑通并留证据；A/B 对比页 [`设计稿与真机对比_v2.html`](设计/设计稿与真机对比_v2.html)。提交 `7df7304`（已推送）。

### ✅ M6 · 存储 / 日志重构（v7 · 技术债）
> 方案：[`归档/存储与日志设计.md`](归档/存储与日志设计.md)（已实施归档）。demo 阶段**直接替换、不做迁移**。Workflow 编排 + 真机 `adb pull` 取证。
- **✅ 应用日志 → 滚动 `.log` 文件**（`files/logs/app-YYYY-MM-DD.log`，单线程串行写、保留 7 天）+ 内存尾窗供「应用日志」屏；崩溃 handler 同步落盘（`appendBlocking`）。真机验证含 `am crash` → `ERROR [crash]` 行。提交 `f38ce01`。
- **✅ 用户 → SQLite（SQLDelight 2.3.2，KMP 原生）**：`subject` 表 + `app_meta`（currentId，`__default__` 伪用户不入表）；`SubjectRepository` 接口不变；SQLDelight 2.1.0 与 AGP 9 不兼容 → 定版 2.3.2。真机 `adb pull bluetrace.db` 取证全部命中。提交 `2c248a3`。
- **不动**：偏好留 DataStore；会话数据留文件 + manifest；scenes.json 留 assets。

### ✅ M6.1 · 权限 / 环境态加固轮（2026-06-25~26）
- 门控点「去开启蓝牙」未授 CONNECT 崩溃修复（安全 `enableBluetooth` 分流）；开启蓝牙后门控/采集首页状态不刷新修复；**小米软关蓝牙不刷新** → 环境态 Flow-first + 回前台 ON_RESUME 兜底；环境态区分蓝牙「已开启」。
- V4 原型权限屏重排 + 46 屏设计审查（[`设计审查报告_v6.md`](设计/设计审查报告_v6.md)）+ 设计稿截图集/画廊。

### ✅ M6.2 · 真实 BLE + S7 手表控制台（原分支 `feat/s7-device-console`，2026-07-02~06 合入 main）
> **M7 的"传输半边"提前落地**：真实 GATT 链路可用，采集"解码半边"仍等协议冻结（见 M7）。
- **真实 BLE**（`AndroidBleClient` v1，自注精简版）：扫描/连接/断开/Notify/Write；连接页重做（共用过滤条、RSSI 滑块、支持设备置顶、扫描权限门含定位）。
- **S7 手表控制台**（设备维护 DUT 从占位变实功能）：B2A 协议（8B 头 + CRC16-CCITT-FALSE + 多包重组，帧层 543 行测试）——对时（任意时间+时区）、设备信息、写用户、**固件日志拉取** → `Download/BlueTrace/logs` + 应用内查看页；`S7MockWatch` 模拟器供无设备联调。
- **Mock/真实可切换绑定**（合并时落地，审查 s7#1）：`BleBackendSwitch` **默认真实 GATT**，设置页 DEBUG 行「使用 Mock BLE」重启生效——Mock 演示/回归链路保留。
- 合并：本地 `eb47c00` 与远端 PR #2/#3 历史汇合，零内容差异。

### ✅ M6.3 · 代码审查修复线 v8–v10（四波 + 真机回归，2026-07-06）
> 起点：四路并行审查（架构/界面/交互/s7 增量）52 条发现 → [`代码审查报告_20260706.md`](代码审查报告_20260706.md)。四波全部收官，高危/中危主体清零。
- **波次① 数据安全与崩溃加固**（`0c53b25`）：采集循环 IO 异常兜底（磁盘满不再崩进程）、manifest 原子写（会话不再静默消失）、幽灵运行页自动弹回、僵尸前台通知自杀、删除确认框、stop() 幂等。
- **波次③ M7 前置接口债**（`95b409b`）：BleClient/SampleDecoder 补形（适配器状态/演示钩子进接口、decoder 生命周期钩子）、背压契约（trySend+droppedPackets 真实计数、tick 批量刷盘）、Service/Controller 与 Mock 彻底解耦。
- **波次② 反馈契约**（`8480af1`）：**勾选导出做真**（`*_partial.zip`）、导出可取消+模态+IS_PENDING 清理、日志页反馈/空态、权限哑弹闭环、暂停语义澄清、空选防呆。
- **波次④ UI 对齐+可达性**（`210da19`）：红→蓝/紫→主蓝（原型"红独占错误/选中统一主蓝"裁决跟进）、图标盒圆角方 + 按钮胶囊（形状层对齐原型）、长按结束无障碍语义（TalkBack 唯一出路）、5 处 48dp 触控目标、实时流稳定 key。
- **真机回归**（M2101K9C）：10 项全过，证据 [`真机证据/regress_20260706/`](真机证据/regress_20260706/)。
- 遗留低优先项清单见 CHANGELOG v10 条目（架构 4/11/12/14、界面 8/12、交互 13/15、s7 增量中低项），按需另起小轮。

### ✅ M6.4 · 文档与协议规格线（2026-07-06）
- **Docs 中文化重组**：过程文档归档 `归档/`、主文档中文命名、导航页重写（[`README.md`](README.md)）；新增 [`context/BlueTrace开发_上下文.md`](context/BlueTrace开发_上下文.md) 活文档（接手先读）。
- **自研协议**：[`归档/自研协议线_v0/BLE协议帧规格_开发者版.md`](归档/自研协议线_v0/BLE协议帧规格_开发者版.md)（SPEC §4 + .proto 的实现视角：帧布局/位图/4 个实算自回验示例包/状态机）。
- **S7 协议**：[`S7B2A/S7协议共识规格.md`](S7B2A/S7协议共识规格.md)（跨项目共识稿，全字段 file:line 溯源固件双仓）+ 随 s7 合并入库的 protocol-b2a / protocol-zqdata 逐字节规格。
- **ZQDATA·UHTP V1 重设计稿**：[`归档/s7/protocol-zqdata-uhtp-v1.md`](归档/s7/protocol-zqdata-uhtp-v1.md) + [`zqdata_uhtp_v1_draft.proto`](归档/s7/zqdata_uhtp_v1_draft.proto)（离线优先：目录分页/窗口授信/断点续传/CRC32；legacy 共存分流）——**待固件评审冻结**。

### ⬜ M7 · 真实 DUT 采集协议解码（P5）—— **当前唯一硬缺口**
> 范围已收窄：真实 BLE 传输 + S7 控制台已由 M6.2 落地；本里程碑 = **采集数据链路的协议解码**。
- **阻塞**：协议冻结——两条候选路径：① 自研 [`归档/自研协议线_v0/bluetrace_v0.proto`](归档/自研协议线_v0/bluetrace_v0.proto)（v0.1 草案）；② **ZQDATA·UHTP V1**（设计稿已出，待固件评审，见 M6.4）。评审通过其一即可开工。
- 冻结后：写一个 `ProtocolProfile` + 解析器注册进 `ProtocolRegistry` 即接入（注册式架构 02 R1–R3 已落码，v11 波次B；编排层零改动）；采集档 BLE 传输已拍板换 **Nordic Kotlin-BLE**（D2，2026-07-06），替代在 `AndroidBleClient` v1 上自研补齐五件事。
- 标准心率带（HRS 0x180D）不依赖冻结，可先跑通真实 BLE 采集链路。
- 口径：当前真实模式下开始采集只落 raw HEX（source of truth）+ unparseable 告警，属预期。

### ⬜ M8 · iOS（二期）
- KMP `:shared` 已为 iOS 预留（纯 Kotlin、可 JVM 单测；v7 选 SQLDelight 亦为 iOS 复用）；iOS app 未开工。

### ⬜ M9 · 服务器 / 上传 / 远程下发（二期）
- 透传上传、`dataSync` 前台服务类型、远程下发；当前设置页"服务器同步"灰显占位。

### ⏸ 暂不实现（一期范围外）
- 配置A/B（传感器总控 / 设备端算法）；DUT OTA（控制台其余功能已在 M6.2 落地）。
- 离线采集实壳（读 DUT flash 已存数据导入 App，待 UHTP V1 冻结，原型已出流程骨架）；真正运行时 i18n locale 切换；场景词表英文接线（界面#12）。

---

## 质量 / 验证状态
- **构建**：`./gradlew :app:assembleDebug` ✅；`:shared:jvmTest` ✅——controller 10 例（含 IO 异常收尾/竞态 stop/decoder 钩子/tick 刷盘）+ store 7 例（含 manifest 原子性）+ v7 存储用例 + **S7 帧层 4 文件 543 行**（CRC/多包/短帧特例），全绿。
- **真机**（Xiaomi M2101K9C / Android 13）：M1–M6 全链路 + v6/v7 路径（历史轮）；**2026-07-06 回归 10 项全过**（Mock 切换、幽灵运行页、僵尸通知、删除确认、勾选导出 `_partial.zip`、暂停语义、空选防呆、日志空态、崩溃会话归档），证据 [`真机证据/regress_20260706/`](真机证据/regress_20260706/)。**未覆盖**：真实 S7 手表链路（待有手表）、多窗口蓝牙死角（交互#15）。
- **BLE 口径**：默认**真实 GATT**；DEBUG 可切 Mock（重启生效）；采集解码走注册式协议架构（Mock/HRS Profile 已注册），自研 DUT 解码待 M7 冻结。
- **设计参照**：[`设计稿与真机对比_v2.html`](设计/设计稿与真机对比_v2.html)（v6 · 6 对）+ 波次④ 形状层对齐抽查（`19_wave4_home`）；旧版在 [`归档/`](归档/)。

## 下一步（建议优先级）
1. **推动协议冻结解锁 M7**：UHTP V1 设计稿提交固件端评审（或冻结自研 `.proto`，二选一/合流）；冻结前可先用标准心率带跑真实 BLE 采集链路。
2. **s7 增量中项小轮**：写结果确认、多包重组上限、pullLog 完整性标志、对时时区语义（审查 s7 #2–#7，约半天）。
3. 审查遗留低优先项（CHANGELOG v10 清单）按需清理；S7 真表到位后补控制台真机回归。
4. M7 落地后再起 iOS(M8) / 服务器(M9)。
