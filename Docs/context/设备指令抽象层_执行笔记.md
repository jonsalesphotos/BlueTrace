# 设备抽象层 · 执行笔记（活文档）

> 开工：2026-07-14。设计真源：[`../设计/设备指令抽象层_设计.md`](../设计/设备指令抽象层_设计.md)（**V2**，按深度评审修订）。
> 执行模式：**每波一个 subagent 落码（重构波=opus，机械波=sonnet），波间主线审查闸门**——diff 审读、验收命令复跑、接口形状对照设计文档、风格红线（KMP 纯度/半角标点注释/无冗余 cast/i18n）；不过闸不放下一波。
> 通用验收命令：`./gradlew :shared:jvmTest :app:assembleDebug :app:testDebugUnitTest` 全绿；不提交 git。

## 波次状态（V2 口径）

| 波 | 范围一句话 | 执行 | 状态 |
| --- | --- | --- | --- |
| W1 | **BLE 通道抽象**（GattSpec 声明式发现/多订阅/写按特征寻址；= 架构优化 P0 合流；S7 走默认参数零变化） | opus | ✅ 过闸 |
| **W1.5** | **NordicBleClient PoC + 三向开关**（Kotlin-BLE-Library 2.0.0-beta03 实现 BleClient 全契约；DEBUG 开关 自写/Nordic/Mock）——D2 恢复（2026-07-14 用户拍板），评估见 [`../设计/BLE库切换_Nordic评估.md`](../设计/BLE库切换_Nordic评估.md) | opus | ✅ 过闸（MTU bug 修复复验含） |
| **W1.6** | **真机 A/B 等价闸门**（主线+用户）：控制台全功能/OTA 传输/复位回连/停止善后；吞吐对照 ~51.7KB/s 基线；过闸 Nordic 转默认、自写降回退 | 主线 | ✅ 全项通过（三轮）；转默认前置=Kotlin≥2.4 升级（任务在册） |
| W2 | **统一识别**：DeviceProfile/Catalog + confirm 二次确认 + 解码 ProtocolRegistry 由 Catalog 派生 + 扫描层去识别化（只依赖 BleClient 接口，可与 W1.6 真机期并行） | opus | ✅ 过闸 |
| W3 | **控制面分面**：DeviceControl 六面+vendor + S7 包装 + DeviceSessionManager（app 宿主）+ 重复 util 合一 | opus | ✅ 过闸 |
| W4 | **固件升级策略**：FirmwareUpdateStrategy + S7 策略吸收 OtaProvisioner（门控/61s 看门狗语义内迁）+ MultiOtaController 队列壳化 | opus+主线 | ✅ 过闸（agent 中断主线接手收尾） |
| W5 | UI 按分面显隐 + vendor 面 + B2aDetect 引用清零 + 真机回归（主线） | opus+主线 | ✅ 过闸（spec 发现段真机补验挂靠库问题任务） |
| W6 | **异构验收**：Mock 第二协议（不同服务/特征 · 缺 logs 能力 · 不同 OTA 重连行为）；判据=只新增协议包+一行注册 | sonnet | ✅ 过闸，**判据达成——抽象层六波闭环** |

## 各波 prompt 共同段

仓库路径与设计文档（V2）指针；范围 In/Out 显式列举；硬约束——① commonMain 零 `android.*`/`java.*`/JVM API（`String.format` 禁用）② 中文注释半角标点 ③ 无冗余 cast ④ 验收命令全绿+不提交 git ⑤ 真机验证过的坑（写流控/取消清理/固件门控语义）不得动；完成报告=文件清单/设计偏离逐条/测试摘要/下一波交接备注。

## 审查记录

- **2026-07-16 · #25 终局：Nordic 不转默认（用户拍板），孤儿连接立为新主线 ✅**：#25 深挖收尾三轮真机实验（sweep 直接解锁→**证伪撤除**（放行的陈旧 disconnect 读到新一代 gatt 并断开之）；ManagerGen 代际+断开看门狗→机制成立但有三条已确认所有权竞态，**封存 origin/task/25 不合 main**）；**分层观测 fork**（官方 tag beta03 + `DISC_REQUEST/DISC_CALLBACK/DISC_CONSUMED` 三观测点，经复合构建 property 门控接入）四层判决=Framework 从未拒绝 / **丢失在 `discoverServices()` 返回 true 与原生回调之间且仅 Nordic 侧** / SharedFlow 管道清白 / App 消费清白；**A/B（同机同表同栈 hold 不取消）：自写 8 次 0 挂死 vs Nordic 7 次 3 挂死（43%）**；时序相关性=3/3 挂死均 MTU 落在发现在飞期间（只报相关性未证机制）。**用户决定：自写继续默认 / Nordic 留可选实验后端 / #24B 无限期暂停**；上游 issue **[#337](https://github.com/nordicsemi/Kotlin-BLE-Library/issues/337)** 已以用户名义提交。**连带发现（自家架构病，比库缺陷深）：孤儿连接**——9 处连接入口全跑 viewModelScope（两 agent 全量审计坐实），退屏腰斩 connect 后物理连接无人持有（设备停广播→连接/扫描/OTA/控制台/采集**五页同时看不见**，`disconnect(id)` 因 conns 已无该 id 而空转+谎报 DISCONNECTED；全仓无 conns↔registry 对账；Nordic 晚登记有 30s 无句柄窗口，自写早登记天然免疫）。修法=**BleConnectionCoordinator**（app 级连接事务宿主：意图提交/页面只观察/`connect+确认+registry.add` 同一事务/显式断开才取消/底层断开返回前不发 Idle/token CAS 每 token 最多善后一次），**提交 1（类+8 测试）已合 main，提交 2（接线）进行中**。真机证据入库 `Docs/真机证据/nordic25_2026071{5,6}/`（`.gitignore` 的 `*.log` 曾把它们静默挡在库外——`git add -f` 修复，此前"证据已入库"的提交信息为误报）。（`5687737` 提交后全 diff 复核: 0 P0 / 6 P1 / 4 P2 全核实为真并修，206 tests/0 failures）**：六波闸门漏网的高价值发现——① 通用连接入口（ConsoleConnect/DeviceScan）识别归 catalog 后连接仍裸 `connect(device)` 走探测（新协议"显示支持连不上"，Mock 忽略 spec 掩盖）→ 改带 `identify()?.gattSpec`；② W5 identity 泛化（firmwareUpdate != null）与 S7 专属 OTA 工具执行链的缝——ZX 可入队被灌 S7 REQ → 两屏收紧 PROFILE_S7，通用 OTA 分派另立后续；③ spec 通道静默降级（缺写特征/缺 notify 仍 CONNECTED）→ 全量契约缺即失败；④ Nordic 多 notify 只等首路（W1.5 意见①遗留）→ 全就绪才 CONNECTED；⑤ OTA 停止善后回读可变成员（快速重启被旧善后误伤）→ 快照+stopping 门（单/多两处）；⑥ 控制台 op 代际保护（W5 重写丢了旧 identity 守卫）→ attachGen 校验。**教训**：subagent+主线审查闸门对"波与波之间的缝"（W5 泛化 vs W4 执行链、识别 vs 连接路径）盲区明显，跨波集成审查值得纳入流程；Mock 对 spec 的宽容会掩盖通道契约问题（Mock 后续可加严格模式）。红线维度（写流控/门控/回连/善后 scope）Codex 对照核验零回归。
- **2026-07-15 · W6 过闸 ✅ = 抽象层总验收判据达成，六波闭环（sonnet 执行 + 主线审查；独立复跑 205 tests / 0 failures = W5 基线 200 + 验收 5）**：新增 ZX 假协议包 [`shared/.../zx/`]（ZxDeviceProfile 识别面 **matches/confirm 用框架默认实现**反证识别面协议中立 + ZxDeviceControl 四通用分面假数据 **logs/vendor 故意 null** + ZxFirmwareUpdateStrategy **"原地生效"升级**全程零 connect/disconnect）；与 S7 全维度异构（AA00 vs FFE0 / AA01+With-Response vs FFE1+Without / 缺两分面 / 无自复位回连）。**触碰面独立核实**：仅 AppModule catalog 一行插入 + MockBleClient roster 一条数据（各带 1 import），`shared/device`、`protocol/registry`、`s7`、双 BLE client、app VM/UI **框架零改动**（mtime+diff 双证）。验收测试 5 例：识别互不误伤 / acquire 端到端（**通用分面读路径首验证**：info.get()/battery.percent() 走通）/ 升级零断链断言 / FwPackage fail fast / 分面 null 性驱动显隐布尔。**设计 V2 §2 验收口径成立：新增一种协议设备 = 一个协议包 + 一行注册。** 项目初衷"兼容其他指令的设备"自此有可执行证明。
- **2026-07-15 · W5 过闸 ✅（opus 执行 + 主线审查 + 真机回归；独立复跑 200 tests / 0 failures）**：`extract16` 迁通用 [`ble/Uuid16.kt`]（删旧非委托），**B2aDetect 引用 S7 包外清零**（grep 复核过）+ **包环 `protocol.registry→s7` 消除**；[`DeviceConsoleViewModel`] 整体重写为 **DeviceSessionManager 首个运行时消费方**（get 复用→仅重连链路 / acquire 新建；六分面 null 即隐块；S7 专属块经 `vendor as? S7VendorOps`；失败统一 DeviceCommandException，**S7 异常类型从 UI 清零**）；identity 判定三口径精化（可控=controlPlane / 可 OTA=firmwareUpdate / 采集排除=controlPlane，语义正确且前向兼容 W6）。**六条偏离全部接受**——关键是 S7VendorOps 扩展到 13 法（snInfo/battery 全量/devFunc/bond/dateTime/drift/restore/heartbeat/opLog 等，全 S7Console 薄委托，保工程控制台零行为变化；vendor 面本就是 S7 逃生口）。**真机回归（D8F7）**：分面控制台首跑 ✓（acquire→confirm→六分面接线→vendor 富信息 refreshAll 全链读回 FW/Bond/Battery）；断开→黏附保留读值→重连 chip ✓；**gattSpec 声明式路径首次真机触发**（`connect start spec=FFE0` 物理建链 1.3-2.1s + mtu=247 两次 ✓）——发现+订阅段被下述库问题所阻未走完（spec 与 probe 共用发现代码、probe 已充分验证，spec 特有 resolveChannels/confirm 有单测，风险低，补验挂靠库问题任务）。
- **2026-07-15（同日晚）· #25 深挖：根因闭环 ✅（源码级 + 可执行证据；分支 `task/25-nordic-reconnect-hang`）**：结论=**库缺陷，两个 bug 叠在同一段三行代码**——① **触发器**：库丢弃 `gatt.discoverServices()` 的 **false 返回值**（`Peripheral.kt:510` 调用点无 if；接口 `:201` 明确有 Boolean 返回；实现 `NativeExecutor.kt:139` 在原生拒绝启动发现时回 false），已置 `Discovering`(:509) 后回调永不到达 → **状态永久停 Discovering**（=实测的 10s 超时）；② **放大器**：该挂住的发现持有**全进程唯一静态 Mutex 永不释放**（`discoverServices` 是全库唯一用裸 `lock()` 而非 `withLock` 处，:504，无 finally；三条解锁路径 :411/:466/:360 **全依赖事件到达**）→ 库内其余一切 GATT 操作均 `withLock(owner=null)` 排在其后 → 任何设备任何 GATT 操作永久挂起（="重试必挂""仅杀进程可解"）。**三个原假设的判决**：全局单例 ✅坐实（`object`+static Mutex，javap+源码双证）；**"取消订阅的 CCC 写泄漏锁" ❌ 证伪**（写/读/订阅全走 `withLock`=kotlinx 自带 try/finally，测试对照组证明取消即释放）——**故上条按此假设做的"断开换序"缓解方向错了，这解释了"换序后仍复现"**；**"闩不复位" ❌ 证伪**（`servicesDiscovered` 在 :357 复位；`serviceDiscoveryRequested` 是库**故意**不复位，:365-369 注释写明为重连留观察者）。**因果定序的硬证据**：重连时 MTU 协商成功（mtu=247），而 MTU 走 `withLock`（core-android `Peripheral.kt:599`）→ 证明那一刻锁空闲 → **是发现失败制造了幽灵锁，不是锁挡住了发现**。**可执行证据**=[`NordicOperationMutexPinTest`]（app JVM 单测 3 例全绿，**不需真机**，直接驱动库真实 OperationMutex；兼作升版跳闸丝）。**已落规避**=`NordicBleClient.sweepGhostServiceDiscoveryLock`（teardown 后 4s 宽限期仍持锁即判幽灵并强制 unlock；**独立协程**——不可串在 disconnectBounded 后，后者实证可能永不返回）：只拆放大器不修触发器，把"全进程永久瘫痪"降级为"本次失败可重试"；边界=并发发现场景会误解他人锁，本项目会话本就串行故接受。**⏳ 真机未验**。全文档=[`../设计/Nordic重连挂死_根因分析.md`](../设计/Nordic重连挂死_根因分析.md)（md+html），上游 issue 草稿=[`../设计/Nordic重连挂死_issue草稿.md`](../设计/Nordic重连挂死_issue草稿.md)（**待用户过目后由用户提交**）。**剩余**：触发器的原生成因（为何主动断开后重连时原生回 false；候选=`disconnect()` 只 `gatt.disconnect()` 不 `close()`，`NativeExecutor.kt:266-274`）需真机三组对照；验收=同机断开/回连 ≥50 次零挂死且无需开关蓝牙。
- **2026-07-15 · Nordic「主动断开→重连」服务发现挂死：库级问题定性（高复现），两轮缓解 + 深挖立项**〔⚠️ **本条的归因已被上一条 #25 深挖推翻，保留作过程记录；结论以上一条为准**〕：现象=App 主动断开设备后，同进程内对该设备重连物理建链成功（1-2s、mtu 247）但服务发现挂 `Discovering` 10s 超时，且超时 teardown 的 disconnect 亦挂（物理连接残留），重试必挂；force-stop 后首连秒过（发现 1-15ms）；**污染升级**：反复挂死后系统栈也脏（force-stop 后首连也挂），需**开关手机蓝牙**才彻底恢复。probe/spec 两路径都踩（09:22 probe / 10:36、10:41、10:45 spec），排除 W5 引入。~~嫌疑链=W1.5 备案的**库内全局 OperationMutex**：断开时取消订阅 collect 触发库发 CCC disable 写，与断链竞态挂死在锁内~~〔**已证伪**：withLock 自带 finally，取消即释放〕，后续一切 GATT 操作（含新连接服务发现）排队永不执行——与"仅杀进程可解"吻合〔**现象描述正确，机制归因错误**：真凶是服务发现的裸 lock〕；~~另一嫌疑=库 serviceDiscoveryRequested/servicesDiscovered 闩在主动断开路径不复位~~〔**已证伪**〕。**已落缓解**：① teardown/disconnect 底层断开 fire-and-forget（连接页不再冻结、可立即重试，实证生效）；② 断开序改"先物理断链后取消订阅域/关控制面"（DeviceSessionManager.release 换序 + NordicBleClient 两处 launch 内串行）——无害化改进但未根治（换序后仍复现）〔**原因已明**：该换序针对的是已证伪的假设〕。**恢复手段**：开关蓝牙 > 杀 App。深挖（库源码定位闩/上报 Nordic issue/评估绕道）单独立项；**Nordic 转默认（任务 #24）前置增加本项**。现场已恢复（蓝牙重启后连接/发现秒过，控制台正常）。
- **2026-07-15 · W4 过闸 ✅（opus 执行至中段被宿主进程退出中断，主线接手收尾+审查合一；独立复跑 199 tests / 0 failures = W3 基线 194 + 策略级新增 5）**：agent 已落且审读通过——[`FirmwareUpdate.kt`] 通用类型（FwUpdatePhase 粗五阶段/FwUpdateProgress(percent,bytesPerSec,detail)/FwUpdateResult/FwPackage 空 marker + FirmwareUpdateFactory 转正，**签名裁定**：收 device 整对象（自复位回连需 scan 匹配 id+connect 用 address）+ abortScope/reconnectScanMs/onLog；S7 细进度观察 OtaPhase/OtaProgress 是 S7 专属类型**不进通用工厂**，需细观察的消费方直接构造具体策略注入）；[`S7FirmwareUpdateStrategy`] **纯类型皮+映射层**——OtaProvisioner/S7OtaSession/OtaAbort 编排逻辑零改动，run=委托 provisionAndReconnect + 细→粗映射（Downloading→Transferring；WaitingReboot/Scanning/Reconnecting→Applying；ReadingVersion→Verifying）+ 细文案塞 detail，**abort 红线**=otaTransferActive 内部自持（onPhase 写/cancelAndJoin 屏障后读）整段委托 OtaAbort（传输态吞 RESET/永不 STOP/61s 看门狗，禁 PowerOps.reboot 替换）；OtaPackage : FwPackage；S7DeviceProfile.firmwareUpdate 挂工厂；MultiOtaController 壳化（策略下沉+stopBatch 转发 strategy.abort；电量门槛保留短命 S7Console=改动最小裁定）。**主线接手补齐**：OtaTestViewModel 单设备路径下沉策略（**唯一入口达成**——app 层 OtaProvisioner/S7OtaSession/OtaAbort import 清零，仅 KDoc 文字引用）；策略级测试 5 例（成功链+粗映射+detail 透传/FwPackage 类型不符 fail fast 零 BLE 触碰/**传输中 abort 不发 RESET**（silent BLE 挂 REQ ack + cancelAndJoin 后 abort，帧捕获断言）/非传输态发 RESET/未连接跳过）。
- **2026-07-15 · OTA 屏 UI 一轮 ✅（用户 5 条需求，随 W4 收尾同批落地）**：①设备卡就地"断开/重连"chip（仿 DUT 控制台头卡，消费点击不进连接页；连接页本就复用 ConsoleConnect）②删"当前版本"行+进屏自动读版本 ③版本移进度卡"版本 A → B"（**开始 OTA 前读一次** + 回连读一次=仅有的两处读点）④运行自然结束**自动断开**（循环=整批结束才断；手动停止路径 stop() 自断）⑤各轮结果行加**本轮下载平均速度**（VM 在 onOtaPhase 计时：进 Downloading 记起点/首次离开结算，零 shared 改动）。OtaIterationResult 改 UI 壳（ok/detail/avgBps），OtaResult 从 Screen 解耦。真机冒烟（D8F7）：单行卡/断开→重连 chip 切换/连接页复用全过。
- **2026-07-15 · NordicBleClient 连接页卡死 bug 真机撞出并修复 ✅（W1.6 补丁）**：复现链=连接协程被取消（连接页快速返回）→ 底层 GATT 脏 → 重连服务发现挂 **Discovering 10s 超时**（修复注释预判过的"回调丢失"形态）→ teardown 的 `disconnectBounded` 内 `withTimeoutOrNull(3s)` **取消切不掉库 disconnect() 的清理挂起**（body 不结束 withTimeoutOrNull 不返回——结构化并发，连"timed out"日志都打不出）→ connect() 永不返回 → 调用方 busy 永挂，连接页按钮冻结"连接中"且无法重试（真机实证 2 分钟不恢复）。**修**=teardown/disconnect() 的底层断开改 **fire-and-forget**（丢 app 级 scope，保留 3s 限时+日志）：conns/link/服务表收尾**无条件立即到达**，调用方立刻可重试；挂死的断开协程待系统 supervision timeout 断链事件自行完结。真机复验：断开→重连快速切换零冻结。
- **2026-07-14 · W1.6 第三轮：吞吐收尾通过，A/B 闸门全项收官 ✅**：NordicBleClient 逐帧 TX/RX hex 日志加**编译期常量开关 `FRAME_LOG`（默认关）**——hex 字符串构造整块 if 门控（常量折叠零残留），排帧级问题改 true 重编。同 24MB 包（OTA_Overseas_V1.1.04，14 文件 23,967,663B）、同固件起点 1.1.99.07、同机同表重测：**Nordic 传输 463s vs 自写基线 464s——逐秒等价，慢 19% 的元凶坐实=逐帧日志开销**；稳态滑动窗 49–52 KB/s 与基线一致；零切片重传零超时。回连链第三次复验：复位断链→10.5s 扫到广播→**重连 1.8s 一次直达**→读回 1.1.04（与第二轮 1.8s 一致，修复稳定）。**W1.6 记分卡全绿**：连接/MTU/命令面/传输流控/复位断链/复位回连/吞吐 七项全过。**转默认前置工程项**（W1.5 审查意见③）：Kotlin 2.2.10→≥2.4 + 删 `-Xskip-metadata-version-check` + BleBackendSwitch 默认翻转，已入任务。现场：设备留 1.1.04（用户裁定不刷回）；本机 App Nordic 开关保持开（继续积累里程）。
- **2026-07-14 · W3 过闸 ✅（opus 执行，主线审查）**：独立复跑 **194 tests / 0 failures**（29 类；W2 基线 175 + 新增 19 = S7DeviceControlTest 9 + DeviceSessionManagerTest 8 + FormatTest 2）+ app 5/0 + assembleDebug 过。形状对齐设计 V2 §3.2——`DeviceControl` 六分面（**分面 null 即无能力**，无 capabilities 集合）+ `DeviceCommandException`（Timeout/DeviceError/Unsupported；codeName 在协议实现构造点解析，通用层零 S7 码表）+ `S7DeviceControl` 包装 S7Console **原样不改**（mapS7 统一异常转换）+ `S7VendorOps` 四操作（setDateTime/getPerson/setPerson/findWatch）。`DeviceSessionManager` acquire（identify→connect **gattSpec 激活**→confirm 二次确认→缓存）/get/release 语义审读正确，单 Mutex 串行够用（多设备 OTA 本就串行）；测试分支全覆盖（成功/复用/identify 空/连接失败/confirm 失败断开/null 控制面/release 三清）+ 真 S7Profile x MockBleClient 集成 1 例。`discoveredService16s` 四落点核过（接口默认 emptyList；Android/Nordic/Mock 三实现，断链清理齐全）。**Mock 裁定 a 接受**（`[S7DeviceProfile, MockDeviceProfile]`）：与真机同一条识别路径，annotate"已带身份不覆盖"守卫 + S7.dataPlane=null 保证扫描/解码零回归，集成测试坐实。util 合一核过：三处 fmtMB/两处 nowStamp 全走 shared `Format.kt`（纯 Kotlin HALF_UP，禁 String.format 达成）。**审查修正 1 处**：DeviceProfile.kt KDoc 两个全角顿号（agent 自检漏网），主线改半角（纯注释，不触发重验）。**遗留注意**：DeviceSessionManager 是 W1 gattSpec 声明式路径**首个调用方**，该路径真机未跑——回归随 W5；acquire 连接失败分支不调 disconnect（依赖三实现 connect 失败自清理语义，成立）。W4 已按 agent 交接备注派发（整链内迁逐字不动/abort 门控红线禁用 PowerOps.reboot 替换/队列壳化/进度映射 detail 透传/FwPackage marker 不上移 OtaPackage/工厂对称转正）。
- **2026-07-14 · W1.6 第二轮：回连双 bug 修复复验通过 ✅（ota_part 19.6MB，1.1.04→1.1.99.06）**：修复=①连接前预注册 `services()`（库设计的重连姿势：serviceDiscoveryRequested 置位，Connected 即刻由库自行触发发现）+ 收敛条件看齐全部终态（`RemoteServices.Failed` 快速失败不再烧成 10s 超时）+ 幂等兜底；②`disconnectBounded`（NonCancellable 内 3s 硬上限，teardown 收尾无条件到达）。**Bug1 库源码级根因**：`NativeGattCallback.onServicesDiscovered` 在 status!=SUCCESS 或服务数<2 时发 `ServiceDiscoveryFailed`→`Failed(EmptyResult)`（刚复位设备服务表未就绪正中此枝），旧代码 `first{Discovered}` 对 Failed 视而不见。复验：复位断链→扫到广播 11s→**重连 1.8s 一次直达**（connect→mtu=247→服务发现 Discovered→CONNECTED，无 Failed 无重试）→版本 1.1.99.06 读回，完整闭环。**W1.6 剩余一项**：吞吐对照未定论——首轮 42 vs 基线 51.7（慢 19%，主嫌=TX/RX 逐帧日志开销）；第二轮 26 不作数（当时设备跑 1.1.04 Overseas 固件、连接参数与基线不可比）。收尾方案=逐帧日志加 DEBUG 开关（默认关）后在 1.1.99.x 固件上重测一轮。现场已恢复自写默认。**潜在库缺陷备案**：Nordic `OperationMutex` 为全局单例，若真机出现"发现卡 Discovering 后全 App 写冻结"属库缺陷，届时报 issue。
- **2026-07-14 · W1.6 首轮真机 A/B（D8F7，24MB OTA_Overseas 1.1.99.07→1.1.04）**：**传输侧过关**——552s/平均 42 KB/s（基线自写 464s/51.7，慢 ~19%，主嫌疑=TX/RX 逐帧 hex 日志开销，logcat 4.4 万行；零切片重传/零超时，库内流控成立）；复位断链观测正常（TerminatePeerUser）。**回连侧双 bug 卡死打回**：① 复位重连后 `services()` 发现 10s 超时（首连正常；疑 getPeripheralById 复用旧 Peripheral 实例内部状态陈旧）② `teardown` 的 NonCancellable `peripheral.disconnect()` 无超时——Bug① 场景下挂死，60s 预算取消杀不掉 → provisionAndReconnect 整链冻结、UI 卡"重连中"。**刷写本身成功**（自写路径复连读回 1.1.04 确认，手表完好）。现场已恢复自写默认。修复后需第二轮 A/B（重点=复位回连 + 关逐帧日志重测吞吐）。
- **2026-07-14 · W2 过闸 ✅（opus 执行，175 tests/0 failures 主线复跑；两轮真机 OTA 的扫描/投影全程正常坐实）**：DeviceProfile/Catalog 形状对齐设计 V2 §3.1（三分面 marker 占位一次到位）；`annotate` 增"已带身份不覆盖"守卫（保 Mock roster 身份，有测试固化）；**通道键短码统一落地**（`channelKey` 两侧 extract16 归一 + HRS/Mock 注册改短码，5 例路由测试证明真正按通道命中不走 single 兜底——W1 交接必修项闭环）；三 client toScanned 去识别化 + 三 VM 投影打标（真机 A/B 实测扫描页 B2A 徽章/kind 过滤正常）。**审查意见（不阻塞）**：① 包环 `protocol.registry→s7`（DeviceParserHost import B2aDetect）——W5 随 B2aDetect 清零把 extract16 迁通用位置；② Mock 模式良性行为变更（演示 run 的 manifest profileId null→Mock.ID）接受。W3 交接要点（agent 报告）：confirm 接线/gattSpec 激活/三分面填充/Mock 单档案局限/通道短码口径固化。
- **2026-07-14 · W1.5 静态过闸 + 真机冒烟发现 1 bug 打回（opus 执行，主线审查）**：NordicBleClient 契约对齐审读过（取消清理 NonCancellable/连上瞬断竞态有防/写流控直接映射——**库内 OperationMutex 串行+等 CharacteristicWrite 完成事件，源码级证据在类 KDoc**，不叠加自写 Mutex）；坐标纠偏（2.0 group=`no.nordicsemi.kotlin.ble` 非 1.x 的 `no.nordicsemi.android.kotlin.ble`）；client-android-mock 单测不可行（需 Context/Robolectric，硬凑会破坏单文件隔离面）→ 真机验证顶上。**真机冒烟（D8F7，Nordic 开关）**：扫描（B2A 广播提取/MAC 口径一致）→ 连接 2.7s → 服务发现 [1812,FFE0,190E] 探测命中 → 订阅 → 控制台 readAll 全链路 TX/RX 逐帧正常（读回版本 1.1.99.07）✅。**发现必修 bug 已打回**：`negotiatedMtu` 取值时机早于 MTU 协商完成（连接后即取返回 23，而后续 71B notify 证明实际已协商大值）——OTA sliceMax 按 23 算会吞吐灾难，修法=MTU 收敛后才置 CONNECTED。**三条审查意见（不阻塞，记录跟进）**：① 多特征 CONNECTED 判定只等首个 onSubscription（自写=全部 CCC 齐活），W2 多通道前统一；② RX 逐帧 Log.d 在高频采集场景需降级（DEBUG 门控/采样），Nordic 转默认前处理；③ **Kotlin 升 ≥2.4 + 删 `-Xskip-metadata-version-check` = Nordic 转正前置工程项**（Nordic beta03 用 Kotlin 2.4.0 编译，现靠逃生阀消费；compileSdk 已 36→37 编译期升级）。现场已恢复（断开 D8F7、Nordic 开关关回自写默认——MTU 修复落地前防误用）。**MTU bug 已修复并真机复验 ✅**：根因=调用点自伤（日志经 negotiatedMtu 读 conns 表，Conn 未入表恒回退 23；协商本身成功）+顺手封死库内 MtuChanged 事件与请求恢复点的理论竞态（peripheralMtu 直接反推 + 2s 轮询兜底 + 日志挪收敛后）；复验日志 `connected C8:D3:E0:C1:D8:F7 mtu=247` 与自写路径一致。**W1.5 过闸 ✅**，剩 W1.6 真机 A/B（24MB OTA 吞吐/停止善后/扫描回连，需用户在场）。
- **2026-07-14 · W1 过闸 ✅（opus 执行，主线审查）**：diff 逐段审读——GattSpec 路径纯加法（服务定位/写特征登记/CCC **串行订阅状态机**正确），写流控/取消清理/MTU 时序逐字未动；`writeCcc` 抽取字节级等价；`write(char16)` 寻址经 `extract16` 归一（短码直通+大写化，B2aDetect.kt:45 亲验）；Mock 的 characteristicId 分路（S7 命令面=FFE2 / 数据面=null 走 single 兜底）与解码侧零回归论证成立。验收本地复跑 **156 tests / 0 failures**。三条偏离（write 无 withResponse 参数=预裁定、writeCcc 抽取=等价重构、新注释半角=硬约束）全部接受。**高价值交接发现**：解码侧 profile 通道注册用 128bit 全串、BLE 层补填 16bit 短码——`DeviceParserHost.parse` 的 `parsers[key]` **从不命中，全靠单通道 `single` 兜底**掩盖；W2 必修（统一短码口径）。GattSpec 路径尚无真调用方（S7 仍走探测），真机验证并入 W1.6/W2。
- **2026-07-14 · V1 设计被深度评审推翻（开工前）**：V1 只是"指令类适配层"——BLE 通道抽象缺位（AndroidBleClient 硬编码 FFE0/FFE1/FFE2）、双 registry 重复识别、OTA 模型带 S7 语义上移、大接口+向下转型矛盾、会话无应用级宿主。五条全部采纳 → 设计 V2（DeviceProfile 三分面/Catalog 识别一次/FirmwareUpdateStrategy/BleConnection 通道抽象），波次 4→6 重排。**旧 W1 agent 已停止，工作区无残留**（停在探索阶段未写文件，git status 复核过）。教训：设计评审要在派发落码前完成，"UI/编排解耦"不等于"协议可替换"——验收判据必须是"新增协议=只加包+注册"。

## 偏离与决策

（执行中偏离设计文档处记这里，同步回设计文档 Open Questions）
